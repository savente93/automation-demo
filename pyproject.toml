[tool.poetry]
name = "automation-demo"
version = "0.1.0"
description = "\"a dummpy project to show off github automations\""
authors = ["Sam Vente <savente93@gmail.com>"]
license = "MIT"
readme = "README.md"
packages = [{include = "automation_demo"}]

[tool.poetry.dependencies]
python = "^3.11"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
